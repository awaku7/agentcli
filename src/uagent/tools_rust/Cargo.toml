[package]
name = "tools-rust"
version = "0.1.0"
edition = "2021"

[lib]
name = "tools_rust"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.23", features = ["extension-module", "abi3-py311"] }
uuid = { version = "1", features = ["v4"] }
