{
  "type": "function",
  "tool_genre": "index",
  "function": {
    "name": "sln2idx",
    "description": "Parse Visual Studio .sln and .slnx solution files into a numbered index or a selected section.",
    "parameters": {
      "type": "object",
      "properties": {
        "path": {"type": "string", "description": "Path to a .sln or .slnx file."},
        "mode": {"type": "string", "enum": ["index", "section"]},
        "section": {"type": "integer", "description": "Section number for section mode."}
      },
      "required": ["path", "mode"],
      "additionalProperties": false
    }
  }
}
