{
  "en": {
    "tool.description": "Edit a local file as raw bytes (binary). Supports offset write, search/replace, splice (insert/delete), and applying a simple JSON patch. Dangerous: this tool can corrupt files.",
    "tool.system_prompt": "This tool is used for the following purpose: edit a local file as raw bytes (binary).\n\nSECURITY / SAFETY:\n- This tool modifies local files and can corrupt them.\n- Always request confirmation with human_ask before writing.\n- Do not operate on paths outside the current workdir when running under the host agent's safety policies.\n",
    "param.path.description": "Target file path.",
    "param.mode.description": "Edit mode.",
    "param.dry_run.description": "If true, do not modify the file; only report what would change.",
    "param.max_bytes.description": "Reject files larger than this size (bytes).",
    "param.offset.description": "Byte offset (0-based). Required for write/splice.",
    "param.data_hex.description": "Hex string of bytes to write/insert (e.g. 'DEADBEEF' or 'DE AD BE EF').",
    "param.search_hex.description": "Hex string to search for (replace mode).",
    "param.replace_hex.description": "Hex string to replace with (replace mode). Must be same length as search_hex unless allow_resize=true (not supported).",
    "param.occurrence.description": "Which occurrence to replace (1-based). 0 means replace all occurrences (still requires same length).",
    "param.splice_op.description": "Splice operation (splice mode).",
    "param.delete_len.description": "Number of bytes to delete (splice delete).",
    "param.patch_json.description": "JSON string patch (apply_patch mode)."
  },
  "ja": {
    "tool.description": "ローカルファイルをバイナリ（生バイト列）として編集します。オフセット書き換え、検索置換、挿入/削除（splice）、簡易 JSON パッチ適用に対応します。危険: ファイルを破損させる可能性があります。",
    "tool.system_prompt": "このツールは次の目的で使われます: ローカルファイルをバイナリ（生バイト列）として編集します。\n\nSECURITY / SAFETY:\n- このツールはローカルファイルを変更し、破損させる可能性があります。\n- 書き込み前に必ず human_ask で確認を取ってください。\n- ホストエージェントの安全ポリシー下では、カレント workdir の外のパスを操作しないでください。\n",
    "param.path.description": "対象ファイルのパス。",
    "param.mode.description": "編集モード。",
    "param.dry_run.description": "true の場合、ファイルは変更せず、変更内容の報告のみ行います。",
    "param.max_bytes.description": "このサイズ（バイト）を超えるファイルは拒否します。",
    "param.offset.description": "バイトオフセット（0 始まり）。write/splice で必須。",
    "param.data_hex.description": "書き込み/挿入するバイト列の 16 進文字列（例: 'DEADBEEF' または 'DE AD BE EF'）。",
    "param.search_hex.description": "検索する 16 進文字列（replace モード）。",
    "param.replace_hex.description": "置換後の 16 進文字列（replace モード）。allow_resize=true を除き search_hex と同じ長さが必要（allow_resize は未対応）。",
    "param.occurrence.description": "置換対象の出現位置（1 始まり）。0 は全出現を置換（長さは同一が必要）。",
    "param.splice_op.description": "splice 操作（splice モード）。",
    "param.delete_len.description": "削除するバイト数（splice の delete）。",
    "param.patch_json.description": "適用する JSON パッチ文字列（apply_patch モード）。"
  }
}
