<p align="center">
  <img src="https://raw.githubusercontent.com/awaku7/agentcli/main/assets/uag-logo.svg" alt="uag logo" width="720">
</p>

<h1 align="center">uag — универсальный шлюз искусственного интеллекта</h1>

<p align="center">
  <b>U</b>niversal <b>A</b>I <b>G</b>ateway — Ваша среда, ваша свобода.
</p>

<p align="center">
  Операции с файлами / Веб-поиск / Генерация и анализ изображений / Извлечение PDF и Excel / Управление IoT / Интеграция MCP<br>
  24 providers / 3 пользовательских интерфейса / Параллельное выполнение инструментов / Agent Skills торговая площадка
</p>

<p align="center">
  <a href="https://github.com/awaku7/agentcli">GitHub</a>
  ·
  <a href="https://pypi.org/project/uag/">PyPI</a>
  ·
  <a href="README.translations.md">Read this in your language</a>
</p>

______________________________________________________________________

## Почему уаг?

**Освободитесь от привязки к поставщику.** Большинство ИИ-помощников привязывают вас к конкретному поставщику или облачному сервису. уаг другой.

- **Запускается локально** на вашем компьютере. Ваши данные остаются с вами (за исключением вызовов API, которые вы совершаете).
- **Свобода поставщиков**: OpenAI, Claude, Gemini, DeepSeek, Ollama, Azure, Bedrock, HuggingFace... 21 поставщиков, все доступны из единого интерфейса. Переключайтесь между ними путем перенастройки переменных среды — без переустановки и миграции.
- **203 инструмент**: файловый ввод-вывод, веб-поиск, генерация изображений, Gmail, сканирование устройств BLE, интеграция с сервером MCP — **111 из них безопасны для параллельного выполнения** (до 8 выполняются одновременно через пул потоков, настраивается через `UAGENT_PARALLEL_WORKERS`). Когда LLM запускает несколько вызовов инструментов одновременно, uag автоматически распараллеливает их.
- **3 интерфейса + A2A**: интерфейс командной строки, графический интерфейс, веб-интерфейс и протокол взаимодействия агентов. Тот же движок, любой интерфейс.
- **Навыки агента**: установите навыки, созданные сообществом, с торговой площадки. Расширяйте uag бесконечно.

uag — **ваш помощник по искусственному интеллекту на ваших условиях**. Не привязан к провайдеру, не привязан к интерфейсу, не привязан к платформе.

## Быстрый старт

```bash
pip install uag
uag
```

При первом запуске мастер установки проведет вас через настройку поставщика.
См. [docs/ENVIRONMENT.md](ENVIRONMENT.md) для всех переменных среды.

## Функции

### 🧠 Многопровайдерная архитектура

OpenAI / PFN (PLaMo) / Azure / Bedrock / OpenRouter / Ollama / Gemini / Vertex AI / Claude / Grok / NVIDIA / Novita / DeepSeek / Z.AI (Zhipu AI) / HuggingFace / Alibaba Cloud (Qwen) / KIMI (Moonshot AI) / Xiaomi MiMo / LM Studio / MiniMax / Sakana AI (Fugu) / SAKURA AI Engine / Together AI / Vercel AI Gateway

Все поставщики используют один и тот же набор инструментов и интерфейс. Переключитесь, установив `UAGENT_PROVIDER` — никаких изменений кода, никаких отдельных установок.

### ⚡ Параллельное выполнение инструментов

Когда LLM запрашивает несколько инструментов одновременно, uag **автоматически распараллеливает** их.
111 инструментов помечены `x_parallel_safe` и выполняются одновременно через `ThreadPoolExecutor` (8 потоков по умолчанию; установите `UAGENT_PARALLEL_WORKERS` для изменения).

**Пример**: запрос «Узнайте погоду в столицах Северных стран» → LLM запускает `search_web` × 5 стран → все 5 поисковых запросов выполняются параллельно → результаты собираются в один пакет.

Инструменты только для чтения (поиск файлов, вычисление хеша, просмотр каталогов, перевод, запросы к базе данных и т. д.) активно распараллеливаются.

### 🧩 Система плагинов (совместима с Claude Code)

uagent реализует систему плагинов, совместимую с Claude Code. Плагины объединяют навыки, агентов, серверы MCP, перехватчики и многое другое в автономные каталоги с манифестом `.claude-plugin/plugin.json`.

**Поддерживаемые компоненты**: навыки, субагенты, серверы MCP, перехватчики (12 событий жизненного цикла), слэш-команды, стили вывода, пользовательская конфигурация, зависимости, каналы, торговые площадки

**CLI commands**:

```
:plugin list                         # Список установленных плагинов
:plugin install <source> [--scope]
:plugin install <name>@<marketplace>  # Установить с торговой площадки
:plugin remove <name>                # Удалить
:plugin enable/disable <name>        # Включить или отключить
:plugin marketplace add/remove/list  # Управление торговыми площадками
:plugin init <name>                  # Создать каркас нового плагина
```

Подробности смотрите в полной документации. [DEVELOP_PLUGIN.md](../src/uagent/docs/DEVELOP_PLUGIN.md)

### 🔄 Непрерывность сеанса

- **Сменить провайдера в середине сеанса** с `UAGENT_PROVIDER` — история беседы сохраняется.
- **Перезагрузить предыдущие сеансы** с помощью `:load <index>` — продолжите с того места, где остановились.

### 🛠 203 инструмент

- **Облачные API**: `aws_api`, `gcp_api`, `azure_api` — AWS, Google Cloud, and Azure API operations; write operations require explicit confirmation.

| Категория | Инструменты |
|---|---|
| **Операции с файлами** | чтение/запись/создание/удаление/поиск/grep/hash/zip, file_type, parse_eml (файлы .eml) |
| **Интернет** | fetch_url, search_web, скриншот, браузер_playwright |
| **СМИ** | генерировать_изображение, анализировать_изображение, img2img, audio_speech, audio_transcribe |
| **Документы** | Извлечение PDF/PPTX/DOCX/RTF/ODT, структурированное извлечение Excel |
| **Прогноз** | Прогнозирование временных рядов с 9 моделями (AutoARIMA, Prophet, LightGBM, CatBoost, TimesFM и др.), автоматический выбор модели, построение графиков, i18n |
| **Общение** | gmail_send, gmail_read, bluesky, discord_channel, groups_webhook, **pybitchat** (BLE Mesh) — см. [COMMUNICATION.md](COMMUNICATION.md) и [BITCHAT.md](BITCHAT.md) |
| **Интернет вещей** | SwitchBot (Облако + BLE), ECHONET Lite, Matter, UPnP, reverse_geocode |
| **Инструменты для разработчиков** | git_ops, python_compile, lint_format, run_tests, db_query, **29 навигаторов исходного кода (семейство idx)** |
| **MCP** | Подключитесь к внешним серверам MCP, просмотрите список инструментов, выполните |
| **А2А** | Связь между агентами (с другими экземплярами uag или A2A-совместимыми серверами) |
| **Система** | переменные env, системные характеристики, расчет времени, даты, uuid_gen, slugify ||
| **Навигация по источнику** | **29 инструментов idx** для Python, PHP, TypeScript, Java, C#, Dart, C/C++, Rust, Go, Swift, Kotlin, COBOL, VBA, LotusScript, Makefile — получите индекс функции/класса или конкретное определение, не читая весь файл |

### 🖥 4 интерфейса + расширение кода VS

| Режим | Команда | Цель |
|---|---|---|
| **CLI** | `уаг` | Быстрая работа через терминал |
| **Графический интерфейс** | `уагг` | Пользовательский интерфейс рабочего стола через tkinter |
| **Интернет** | `уагв` | Доступ через браузер |
| **Сервер A2A** | `уага` | Протокол Agent2Agent для межагентной связи |
| **Код VS** | — | [Расширение] (VSCODE.md) с панелью чата, объяснением, рефакторингом, исправлением ошибок и древовидным представлением инструментов |

См. [VSCODE.md](VSCODE.md) для получения подробной информации о расширении VS Code — установке, командах, сочетаниях клавиш и настройке.

### 🏠 Управление устройствами Интернета вещей

- **Важно**: проверка топологии контроллера/моста/устройства только для чтения.

См. [IOT_USECASE.md](IOT_USECASE.md).

### 🎯 Биржа навыков агентов

`:skills mp_search` для просмотра [SkillsMP](https://skillsmp.com) и [ClawHub](https://clawhub.ai) для поиска навыков сообщества.
Устанавливайте и расширяйте возможности uag на лету.

### 🤖 Автопилот (`:auto`)

uag может **автономно добиваться цели на протяжении нескольких раундов LLM**. Идеально подходит для сложных, многоэтапных задач, требующих итеративной доработки.

- **Как это работает**: каждый раунд состоит из основного запроса (шаг А), за которым следует заключение рецензента (шаг Б), которое принимает решение «ЗАВЕРШИТЬ или ПРОДОЛЖИТЬ?»
- **Тот же поставщик, тот же API**: по мнению рецензента, используется тот же путь к коду, что и в основном запросе, включая поддержку API ответов.
- **Отдельное судейство LLM** (необязательно): установите `UAGENT_AP_PROVIDER`, чтобы использовать другого поставщика/модель для рецензента (например, использовать более дешевую модель для судейства).
- **Выйти в любое время**: нажмите клавишу «x», чтобы немедленно остановиться, даже в середине ответа. Или позвольте рецензенту решить, когда цель будет достигнута.
- **Настраивается**: `--max-rounds N` для контроля бюджета.

Полную документацию см. в [README_AUTO.md](README_AUTO.md).

### 🧩 Пакетный менеджер состояний

uag может отслеживать ход выполнения длительных многофайловых задач. Когда LLM обрабатывает десятки файлов, `batch_state` сохраняет на диске список ожидающих, завершенных и неудавшихся файлов. Если сессия заканчивается или время раунда истекает, следующий запуск возобновляется с того места, где он был остановлен — ничего не теряется.

### 🛡 Человек в курсе

Human_ask позволяет LLM приостанавливать работу и запрашивать ваше подтверждение перед выполнением разрушительных операций (удаление файлов, перезапись, команды оболочки). Вы остаетесь под контролем.

### 🛑 Прерывание (клавиша c/кнопка «Стоп»)

Остановите генерацию ответа LLM в любой момент и подайте команду остановки обратно в LLM.

| Интерфейс | Как прервать |
|---|---|
| **CLI** | Нажмите клавишу `c` во время потоковой передачи LLM — текущий ответ останавливается, и `"Стоп"` отправляется как пользовательское сообщение, поэтому LLM отвечает соответствующим образом |
| **ВЕБ-интерфейс** | Нажмите красную кнопку **■ Стоп** (появляется автоматически во время обработки LLM) |
| **Графический интерфейс рабочего стола** | Нажмите красную кнопку \*\*\*\*\* (появляется автоматически во время обработки LLM) |

Прерывание работает как «быстрая инъекция»: вместо простого прерывания оно передает «Стоп» обратно в LLM в виде пользовательского сообщения, позволяя ему корректно завершить или подтвердить прерывание.

Нажмите клавишу «x», чтобы выйти из режима автопилота (см. [README_AUTO.md](README_AUTO.md)).

### 🕵️ Автоматизация браузера и веб-инспектор

Два взаимодополняющих инструмента, основанных на драматурге:

- **browser_playwright**: автоматизируйте реальные сеансы браузера — перемещайтесь, щелкайте, заполняйте формы, извлекайте данные, обрабатывайте многостраничные потоки. Работает без головы или с головой.
- **playwright_inspector**: записывайте переходы браузера, делайте снимки DOM и снимки экрана на каждом этапе. Полезно для отладки веб-взаимодействий или аудита изменений страниц с течением времени.

### 🔄 Динамическая загрузка инструментов

«tool_catalog» и «tool_load» позволяют обнаруживать и включать инструменты во время выполнения.
Не нужно загружать все при запуске — активируйте только то, что вам нужно, и тогда, когда вам это нужно.

### 🦀 Rust Native Tools

Для повышения производительности `uuid_gen` и `slugify` реализованы на Rust (через PyO3).

### 🌐 i18n / L10n

日本語 / Английский / 简体中文 / 繁體中文 / 한국어 / Español / Français / Русский / и многое другое.
Установите `UAGENT_LANG` для переключения. См. [ADD_LOCALE.md](../src/uagent/docs/DEVELOP_I18N.md), чтобы добавить новую локаль.

Переводы этого README доступны в [docs/README.translations.md](README.translations.md).

### 🔒 Зашифрованные переменные среды

Храните ключи и секреты API в .env.sec — зашифрованном файле .env.
Управляйте с помощью `uag_envsec`.

## Конфигурация и подробности

- **Переменные среды**: [docs/ENVIRONMENT.md](ENVIRONMENT.md)
- **Мастер установки**: `python -m uagent.setup_cli`
- **Зашифрованный env**: `uag_envsec` — зашифровать `.env` как `.env.sec`
- **API ответов**: установите `UAGENT_RESPONSES=1` для режима API ответов (OpenAI/Azure/Bedrock/OpenRouter/Ollama/Alibaba/LM Studio/Sakana AI). Автоматически включается для Sakana AI (Fugu).
- **Документация для разработчиков**: [DEVELOP.md](../src/uagent/docs/DEVELOP.md)
- **Поток инструментов**: [TOOL_FLOW.md](../src/uagent/docs/TOOL_FLOW.md)
- **Небольшие советы по программе LLM**: [SLM_TIPS.md](SLM_TIPS.md)

## Философия проекта

uag стремится стать **вашим ИИ, на вашей машине, на ваших условиях**.

- Нет зависимости от SaaS — работает локально.
- Нет привязки к провайдеру — переключайтесь в любое время
- Нет привязки пользовательского интерфейса — CLI/GUI/Web/A2A
- Нет привязки к функциям — расширяйте возможности с помощью инструментов и навыков.

Бесплатный AI-агент, свободный от привязки к поставщику.

### ✨ Создайте свои собственные инструменты

[ru.md](TOOL_CREATOR_GUIDE.ru.md)
Пошаговое руководство доступно здесь.

## Участие в разработке

Вклады приветствуются! Отчеты об ошибках, предложения по функциям, улучшения документации, переводы и запросы на включение — все это приветствуется.

- **Issues**: Откройте проблему GitHub для ошибок или запросов на добавление функций.
- **Запросы на включение**: Создайте fork репозитория, внесите изменения и отправьте PR. Настройки разработки и рекомендации приведены в [DEVELOP.md](../src/uagent/docs/DEVELOP.md).

Realtime Голос и AEC3

## Realtime голосовой режим поддерживает полнодуплексный вход/выход микрофона и динамика. Если серверная часть AEC3 отсутствует, uag автоматически устанавливает pywebrtc-audio.

**Поставщики реального времени**: OpenAI Realtime, Azure OpenAI GPT Realtime, Google Gemini Live, xAI Grok Voice и Amazon Bedrock Nova Sonic. SDK двунаправленной потоковой передачи Bedrock устанавливается автоматически только при выборе Bedrock.

```bat
python scheck.py realtime
```

AEC3 использует фактический сигнал микрофона (ближний) и звук, фактически отправленный на динамик (дальний). Включайте диагностику только при обнаружении проблем со звуком.

```bat
set UAGENT_REALTIME_AUDIO_DEBUG=1
python scheck.py realtime
```

### OpenAI Realtime Function Calling

OpenAI Realtime поддерживает интеграцию Function Calling с ограничениями по безопасности. Текущий адаптер автоматически предоставляет функцию get_current_time только для чтения. Деструктивные инструменты и элементы управления устройствами требуют явного списка разрешений и потока подтверждения. Grok Realtime использует отдельный адаптер и не использует этот путь Function Calling, характерный для OpenAI.
