<p align="center">
  <img src="https://raw.githubusercontent.com/awaku7/agentcli/main/assets/uag-logo.svg" alt="uag logo" width="720">
</p>

<h1 align="center">uag — универсальный шлюз искусственного интеллекта</h1>

<p align="center">
  <b>У</b>универсальный <b>A</b>I <b>G</b>шлюз — ваша среда, ваша свобода.
</p>

<p align="center">
  Работа с файлами, веб-поиск, создание и анализ изображений, извлечение файлов PDF и Excel, управление Интернетом вещей, интеграция MCP<br>
  Более 15 поставщиков / 3 пользовательских интерфейса / Параллельное выполнение инструментов / Рынок навыков агентов
</p>

<p align="center">
  <a href="https://github.com/awaku7/agentcli">GitHub</a>
  ·
  <a href="https://pypi.org/project/uag/">PyPI</a>
  ·
  <a href="https://github.com/awaku7/agentcli/blob/main/docs/README.translations.md">Прочитайте это на своем языке</a>
</p>

---

## Почему uag?

**Освободитесь от привязки к поставщику.** Большинство ИИ-помощников привязывают вас к конкретному поставщику или облачному сервису. uag другой.

- **Запускается локально** на вашем компьютере. Ваши данные остаются с вами (за исключением вызовов API, которые вы совершаете).
- **Свобода поставщиков**: OpenAI, Claude, Gemini, DeepSeek, Ollama, Azure, Bedrock... Более 15 поставщиков, все доступны из единого интерфейса. Переключайтесь между ними путем перенастройки переменных среды — без переустановки и миграции.
- **131 инструментов**: ввод-вывод файлов, веб-поиск, создание изображений, сканирование устройств BLE, интеграция с сервером MCP — **76 из них работают параллельно**. Когда LLM одновременно запускает несколько вызовов инструментов, uag автоматически выполняет их через пул потоков.
- **4 интерфейса + A2A**: интерфейс командной строки, графический интерфейс, веб-интерфейс и протокол агент-агент. Тот же движок, любой интерфейс.
- **Подготовка к Интернету вещей**: SwitchBot, ECHONET Lite, Matter, UPnP — управляйте своими домашними устройствами с помощью искусственного интеллекта.
- **Навыки агента**: установите навыки, созданные сообществом, с торговой площадки. Расширяйте uag бесконечно.

uag — **ваш помощник по искусственному интеллекту на ваших условиях**. Не привязан к провайдеру, не привязан к интерфейсу, не привязан к платформе.

## Быстрый старт

```bash
pip install uag
uag
```

При первом запуске мастер установки проведет вас через настройку поставщика.
См. [ENVIRONMENT.md](https://github.com/awaku7/agentcli/blob/main/ENVIRONMENT.md) для всех переменных среды.

## Особенности

### 🧠 Многопровайдерная архитектура

OpenAI / Azure / Bedrock / OpenRouter / Ollama / Gemini / Vertex AI / Claude / Grok / NVIDIA / DeepSeek / Alibaba Cloud (Qwen) / KIMI (Moonshot AI) / Xiaomi MiMo / LM Studio / MiniMax / **Sakana AI (Fugu)**

Все поставщики используют один и тот же набор инструментов и интерфейс. Переключитесь, установив `UAGENT_PROVIDER` — никаких изменений кода, никаких отдельных установок.

### ⚡ Параллельное выполнение инструментов

Когда LLM запрашивает несколько инструментов одновременно, uag **автоматически распараллеливает** их.
76 инструментов имеют пометку «x_parallel_safe» и выполняются одновременно через 4-поточный «ThreadPoolExecutor».

**Пример**: запрос «Узнайте погоду в столицах Северных стран» → LLM запускает `search_web` × 5 стран → все 5 поисковых запросов выполняются параллельно → результаты собираются в один пакет.

Инструменты только для чтения (поиск файлов, вычисление хеша, просмотр каталогов, перевод, запросы к базе данных и т. д.) активно распараллеливаются.

### 🔄 Непрерывность сеанса

- **Смена провайдера в середине сеанса** с помощью UAGENT_PROVIDER — история разговоров сохраняется.
- **Обновите прошлые сеансы** с помощью `:load <index>` — продолжайте с того места, где остановились.
- **Кэширование результатов инструмента** позволяет избежать избыточного повторного выполнения при повторении одного и того же вызова инструмента.

### 🛠 131 инструментов

| Категория | Инструменты |
|---|---|
| **Операции с файлами** | чтение/запись/создание/удаление/поиск/grep/хэш/zip |
| **Интернет** | fetch_url, search_web, скриншот, браузер_playwright |
| **СМИ** | генерировать_изображение, анализировать_изображение, img2img, audio_speech, audio_transcribe |
| **Документы** | Извлечение PDF/PPTX/DOCX/RTF/ODT, структурированное извлечение Excel |
| **Интернет вещей** | SwitchBot (Облако + BLE), ECHONET Lite, Matter, UPnP |
| **Инструменты для разработчиков** | git_ops, python_compile, lint_format, run_tests, db_query, **13 idx tools** |
| **MCP** | Подключитесь к внешним серверам MCP, просмотрите список инструментов, выполните |
| **А2А** | Связь между агентами (с другими экземплярами uag или A2A-совместимыми серверами) |
| **Система** | переменные env, системные характеристики, расчет времени, даты |

### 🖥 3 интерфейса + A2A + VS Code

| Режим | Команда | Цель |
|---|---|---|
| **CLI** | `uag` | Быстрая работа через терминал |
| **Графический интерфейс** | `uagг` | Пользовательский интерфейс рабочего стола через tkinter |
| **Интернет** | `uagв` | Доступ через браузер |
| **Сервер A2A** | `uagа` | Протокол Agent2Agent для межагентной связи |
| **VS Code** | — | Extension (Chat Panel, Explain, Refactor, Fix Error, Tools Tree View) — see [VSCODE.md](VSCODE.md) |

### 🏠 Управление устройствами Интернета вещей

- **SwitchBot**: облачное пакетное управление и сканирование/управление BLE.
- **ECHONET Lite**: обнаружение и управление бытовой техникой (кондиционер, освещение, водонагреватели и т. д.) в локальной сети.
- **Важно**: проверка топологии контроллера/моста/устройства только для чтения.
- **UPnP**: обнаружение устройств и переадресация портов IGD.

См. [IOT_USECASE.md](https://github.com/awaku7/agentcli/blob/main/IOT_USECASE.md).

### 🎯 Биржа навыков агентов

`:skills mp_search` для просмотра [SkillsMP](https://skillsmp.com) и [ClawHub](https://clawhub.ai) для поиска навыков сообщества.
Устанавливайте и расширяйте возможности uag на лету.

### 🧩 Пакетный менеджер состояний

uag может отслеживать ход выполнения длительных многофайловых задач. Когда LLM обрабатывает десятки файлов, `batch_state` сохраняет на диске список ожидающих, завершенных и неудавшихся файлов. Если сессия заканчивается или время раунда истекает, следующий запуск возобновляется с того места, где он был остановлен — ничего не теряется.

### 🛡 Человек в курсе

Human_ask позволяет LLM приостанавливать работу и запрашивать ваше подтверждение перед выполнением разрушительных операций (удаление файлов, перезапись, команды оболочки). Вы остаетесь под контролем.

### 🛑 Interrupt (c-key / Stop button)

Stop LLM response generation at any time and inject a stop command back to the LLM.

| Interface | How to interrupt |
|---|---|
| **CLI** | Press `c` key during LLM streaming -- the current response stops, and `"Stop"` is sent as a user message so the LLM responds accordingly |
| **WEB UI** | Click the red **■ Stop** button (appears automatically during LLM processing) |
| **Desktop GUI** | Click the red **■** button (appears automatically during LLM processing) |

The interrupt works as "prompt injection": instead of just aborting, it feeds `"Stop"` back to the LLM as a user message, allowing it to gracefully conclude or acknowledge the interruption.

Press `x` key to exit auto-pilot mode (see `:auto` command).

### 🕵️ Автоматизация браузера и веб-инспектор

Два взаимодополняющих инструмента, основанных на драматурге:

- **browser_playwright**: автоматизируйте реальные сеансы браузера — перемещайтесь, щелкайте, заполняйте формы, извлекайте данные, обрабатывайте многостраничные потоки. Работает без головы или с головой.
- **playwright_inspector**: записывайте переходы браузера, делайте снимки DOM и снимки экрана на каждом этапе. Полезно для отладки веб-взаимодействий или аудита изменений страниц с течением времени.

### 🔄 Динамическая загрузка инструментов

«tool_catalog» и «tool_load» позволяют обнаруживать и включать инструменты во время выполнения.
Не нужно загружать все при запуске — активируйте только то, что вам нужно, и тогда, когда вам это нужно.

### 🌐 i18n / L10n

日本語 / Английский / 简体中文 / 繁體中文 / 한국어 / Español / Français / Русский / и многое другое.
Установите `UAGENT_LANG` для переключения. См. [ADD_LOCALE.md](https://github.com/awaku7/agentcli/blob/main/src/uagent/docs/ADD_LOCALE.md), чтобы добавить новую локаль.

Переводы этого README доступны в [docs/README.translations.md](https://github.com/awaku7/agentcli/blob/main/docs/README.translations.md).

### 🔒 Зашифрованные переменные среды

Храните ключи и секреты API в .env.sec — зашифрованном файле .env.
Управляйте с помощью `uag_envsec`.

## Конфигурация и подробности

- **Переменные среды**: [ENVIRONMENT.md](https://github.com/awaku7/agentcli/blob/main/ENVIRONMENT.md)
- **Мастер установки**: `python -m uagent.setup_cli`
- **Зашифрованный env**: `uag_envsec` — зашифровать `.env` как `.env.sec`
- **API ответов**: установите `UAGENT_RESPONSES=1` для режима API ответов (OpenAI/Azure/Bedrock/OpenRouter/Ollama/Alibaba/LM Studio/Sakana AI).
- **Документация для разработчиков**: [DEVELOP.md](https://github.com/awaku7/agentcli/blob/main/src/uagent/docs/DEVELOP.md)
- **Небольшие советы по программе LLM**: [SLM_TIPS.md](https://github.com/awaku7/agentcli/blob/main/SLM_TIPS.md)

## Философия проекта

uag стремится стать **вашим ИИ, на вашей машине, на ваших условиях**.

- Нет зависимости от SaaS — работает локально.
- Нет привязки к провайдеру — переключайтесь в любое время
- Нет привязки пользовательского интерфейса — CLI/GUI/Web/A2A
- Нет привязки к функциям — расширяйте возможности с помощью инструментов и навыков.

Бесплатный AI-агент без привязки к поставщику.